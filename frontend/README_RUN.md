# Frontend Run

## Local run

```bash
cd frontend
npm install
npm run dev
```

Environment variable:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
```

The first screen reads:

- `/campaigns/companies`
- `/campaigns/running`
