# Infrastructure

This directory is reserved for:

- Docker and Compose files
- reverse proxy configuration
- environment templates
- deployment scripts

Infrastructure files will be added after the backend and frontend bootstrap is in place.

Current files:

- `nginx.conf`
- `DEPLOYMENT.md`
