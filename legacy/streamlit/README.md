# Legacy Streamlit

Reserved location for the legacy Streamlit application as the migration progresses.

Current status:

- the production-safe Streamlit code still runs from the repository root
- this directory exists to make the target repository layout explicit
